# --- Postgres ---
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# SQLAlchemy DSN (asyncpg)
DATABASE_URL=postgresql+asyncpg://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@db:5432/$(POSTGRES_DB)

# --- JWT / Security ---
SECRET_KEY=
ALGORITHM=
ACCESS_TOKEN_EXPIRE_MINUTES=
REFRESH_TOKEN_EXPIRE_MINUTES=

# --- FastAPI / Uvicorn ---
BACKEND_PORT=
WORKERS=
APP_ENV=
LOG_LEVEL=

# --- Redis / Cache ---
REDIS_URL=redis://redis:6379

# --- Build target (dev|prod) для docker-compose ---
BUILD_TARGET=